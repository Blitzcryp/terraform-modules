# ONE output: `manifest`, an object collecting id / arn / name + anything a caller needs.

# output "manifest" {
#   description = "All outputs of this module, collected on a single object."
#   value = {
#     id  = aws_<resource>.this.id
#     arn = aws_<resource>.this.arn
#   }
# }
